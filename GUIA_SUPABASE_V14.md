# Guia Supabase V14

Execute a migration:

```sql
supabase/migrations/024_vf_nexus_v14_comercial.sql
```

Depois valide:
- tabela `catalogos_publicos`;
- tabela `planos_saas`;
- tabela `assinaturas_saas`;
- tabela `importacoes_dados`;
- tabela `suporte_chamados`;
- função `vf_registrar_venda_completa_v14`;
- policies de catálogo público.
